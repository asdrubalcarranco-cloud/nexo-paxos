cd /path/to/nexo-paxos
git init
git add .
git commit -m "Primer commit: nexo-paxos public repository"
git branch -M main
git remote add origin https://github.com/asdrubalcarranco-cloud/nexo-paxos.git
git push -u origin main
